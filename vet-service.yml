spring:
  application:
    name: vet-service

  datasource:
    url: jdbc:mysql://localhost:3306/petclinic_vet
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format-sql: true
    show-sql: true
    open-in-view: false
